// array is like ghar ka fruit plate only one type of fruit like only one data type is used in single array only integer, only float 
#include <stdio.h>

int main()
{
    int Arr[5] = {10,20,30,40,50};

    printf("%d\n",Arr[0]);   //Arr of 0
    printf("%d\n",Arr[1]);
    printf("%d\n",Arr[2]);
    printf("%d\n",Arr[3]);
    printf("%d\n",Arr[4]);
    return 0;
}